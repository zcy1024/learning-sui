# 第二十二章 · 示例代码（PAS）

## `pas_lab/`

占位模块（版本号常量），与全书目录对齐、可 `sui move build`；完整 PAS 策略与命名空间请在独立协议仓库实现。

```bash
cd pas_lab
sui move build
```

本地练习可结合 **`../../15_nft_kiosk/code/simple_nft/`**、**`../../14_tokens/code/silver_coin/`** 扩展。
