# 第十八章 · 示例代码（安全）

## `security_lab/`

最小 **Capability** 模式（`AdminCap` + 受控读取），可编译自查；审计清单仍以正文为准。

```bash
cd security_lab
sui move build
```

亦可结合 **第五章** 各节包、`../../12_patterns/code/patterns_lab/` 做交叉审查。
